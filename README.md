# Assignment1_Data221_W26
Data221_Question1 simply requires one to write a python program that multiplies consecutive integers starting form 1 until the product first becomes greater than a given threshold value

Data221_Question2: One needs to define a function that recieves a list of strings and returns a dictionary

Data221_Question3: Define a function that computes x^y and with the given list of pairs (x,y) to use a for loop unpackaging to call the function

Data221_Question4: We are given a list of random numbers between 0 and 1 and a random number x, we have to sort the list, find all indices where the list value is greater than or equal to x and then print out the sorted list, the vlaue of x and the first matching index if it exists

Data221_Question5: Write a function that takes the radii of two circles and validates that both radii are positive integers, computes the area of each circle, returns the percentage of the larger circle's area that can be covered by the smaller circle

Data221_Question6: Define a function that recieves a list of numbers and returns a dictionary where each key is unique value from the list, each value os the percentage of elements in the list that are less then or equal to that key.

Data221_Question7: We are required to write a function that cinverts a given number of seconds into hours, minutes, seconds, am or pm and return a formatted string

Data221_Question8: Here, install pandas package and import it using alias pd and create a DataFrame
